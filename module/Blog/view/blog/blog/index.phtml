<?php $this->headTitle()->prepend($page->getMetaTitle()); ?>
<?php $this->headMeta()->appendName('keywords', $page->getMetaKeywords()); ?>
<?php $this->headMeta()->appendName('description', $page->getMetaDescription()); ?>

<div class="index">
<?php if ($index->count() > 0): ?>
    <?php foreach($index as $key => $post): ?>
        <?php $href = $this->navigation('Navigation')->findOneBy('id_blog_post', $post->getId())->getHref(); ?>

        <article>

        	<div class="header">
        		<h2>
        		    <a href="<?php echo $href; ?>" title="<?php echo $this->escapeHtml($post->getTitle()) ?>">
                        <?php echo $this->escapeHtml($post->getTitle()) ?>
        		    </a>
        		</h2>
        		<h3><?php echo $this->escapeHtml($post->getSubtitle()) ?></h3>
        	</div>
        	
        	<p><?php echo $this->escapeHtml($post->getLead()) ?></p>
        	
        	<a title="Read article: <?php echo $this->escapeHtml($post->getTitle()) ?>" href="<?php echo $href; ?>">Read more...</a>
        	
        	<footer>
                <div>
                    <span class="date">
                        Date added: <time datetime="<?php echo $post->getDate() ?>"><?php echo $this->date($post->getDate()) ?></time>
                    </span>

                    <span class="comments">
                        <img alt="Comments" src="/img/icons/comments.png">
                            <?php echo $post->getComments(); ?> response<?php echo ((1 != $post->getComments()) ? 's' : ''); ?>
                    </span>
                </div>
            </footer>

        </article>
    <?php endforeach; ?>
<?php else: ?>
    <h1>Eh..?</h1>
    <p>What exactly are you lookin' for?</p>
<?php endif; ?>
</div>